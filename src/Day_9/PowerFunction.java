package Day_9;

public class PowerFunction {

    //function to calculate power
    public double calculatePower(int A, int B){
        return Math.pow(A, B);
    }

    //function to return power for given value
    public double powerFunction(int A, int B){
        return calculatePower(A, B);
    }
}
